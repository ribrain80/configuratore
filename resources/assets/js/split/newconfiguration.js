/**
 * Gestione del package template newconfiguration
 * @type {Vue}
 */
var newconfiguration = new Vue({

    el: 'newconfiguration',

    data: {
        
    },

    methods: {
       
        pagereload: function () {
            
        }
    },

    mounted() {
        console.log( "new conf component initialized" );
    }
});
